# クイック統合チェック

## ✅ コピペ前に確認すること

### 1. コードの形式
- [ ] Astro形式（`.astro`）になっている
- [ ] または、HTML + CSS + Script が含まれている

### 2. 重要な部分
- [ ] `<script>` タグが含まれている（アニメーション用）
- [ ] `<style>` タグが含まれている
- [ ] 画像パスが `/images/` で始まっている

### 3. 既存の機能
- [ ] GSAPアニメーションのスクリプトが含まれている
- [ ] `merge-img-wrapper`, `merge-text-wrapper`, `tategaki-char` のクラスが使われている

---

## 🚀 安全な統合方法

### 方法1: 完全置き換え（推奨）
**条件**: ジェミニが完全なAstroコンポーネントを提供した場合

1. `src/components/HorizontalScroll.astro` を開く
2. **全体を選択して削除**
3. ジェミニのコードを**そのまま貼り付け**
4. 保存して確認

### 方法2: 部分置き換え
**条件**: HTML部分だけを受け取った場合

1. `<section class="horizontal-wrapper">` から `</section>` までを置き換え
2. `<style>` セクションを更新
3. **`<script>` セクションは既存のものを維持**（重要！）

---

## ⚠️ 注意事項

- **スクリプト部分は重要**: 既存のGSAPアニメーションを維持する必要があります
- **画像パス**: すべて `/images/` で始まっているか確認
- **バックアップ**: 統合前に既存ファイルをバックアップ推奨

---

## 💡 推奨手順

1. **既存ファイルをバックアップ**
2. **ジェミニのコードを `gemini-horizontal.html` に貼り付け**
3. **私に「HorizontalScrollのコードを確認して統合して」と伝える**
4. **私が安全に統合します**
